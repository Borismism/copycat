# GCP Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=europe-west4
ENVIRONMENT=dev

# Service URLs (for local development, leave empty to use local discovery service)
DISCOVERY_SERVICE_URL=http://localhost:8081

# Firestore Emulator (for local development)
FIRESTORE_EMULATOR_HOST=localhost:8200

# BigQuery Dataset
BIGQUERY_DATASET=copycat_dev
